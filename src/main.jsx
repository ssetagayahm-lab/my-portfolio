import React from "react";
import ReactDOM from "react-dom/client";
import PortfolioMindMap from "./PortfolioMindMap.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <PortfolioMindMap />
  </React.StrictMode>
);
